---
name: perf-tune
description: Analyze code for performance by identifying one bottleneck or hot path at a time, explaining why it matters, and proposing the exact optimized version. Waits for approval before moving to the next. Use when user wants to optimize performance, profile code, or mentions "perf-tune".
---

Analyze this code for performance. Identify ONE bottleneck or hot path at a time. Explain why it matters, then propose the exact optimized version. Wait for my approval before the next bottleneck.

If a bottleneck can be better understood by exploring the codebase, explore the codebase instead.
